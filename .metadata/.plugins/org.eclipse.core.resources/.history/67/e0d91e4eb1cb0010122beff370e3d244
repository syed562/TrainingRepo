package com.app.repo;

import java.util.List;

import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.CrudRepository;

import com.app.models.Question;

public interface QuestionRepo extends CrudRepository<Question, Integer> {

	
List<Question> findByCaseIgnoreCategory(String category);


@Query(
		  value = "SELECT * FROM question WHERE LOWER(category) = LOWER(:category) ORDER BY RAND() LIMIT :num",
		  nativeQuery = true
		)
List<Question> findByCaseIgnoreCategoryAndNum(String category, int num);
}
